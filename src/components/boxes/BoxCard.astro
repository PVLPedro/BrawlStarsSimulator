---
const { box, imgAlt } = Astro.props;
---

<div class="grow flex items-center h-32 min-w-80 gap-xl p-bs rounded-sm bg-bg-secondary">
    <img src={`assets/images/${box}.png`} alt={imgAlt} class="h-20 w-20" />
    <span class="flex flex-col flex-1 gap-xs">
        <slot />
    </span>
</div>